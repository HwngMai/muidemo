import { createTheme } from "@mui/material";
export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#3234de",
    },
    secondary: {
      main: "#6e5623",
    },
    myColor: {
      main: '#00D993'
    }
  },
});
